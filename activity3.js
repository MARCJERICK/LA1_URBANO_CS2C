// Declare a boolean variable indicating if JavaScript is a programming language
let isJsProgrammingLanguage = true;

// Declare a boolean variable indicating if JavaScript is hard
let isJsHard = false;

// Declare a variable to store a favorite number
let favnumb = 16;

// Declare a variable with an undefined value
let favNumb = undefined;

// Log the values of all the variables, ensuring they are separated by commas
console.log(isJsProgrammingLanguage, isJsHard, favnumb, favNumb);
